<!-- =================================================================
     Kiro Feature Spec: Design
     Technical architecture for the feature.
     Generated by Kiro from requirements, then refined by human.
     ================================================================= -->

# Design: [Feature Name]

## Technical Approach

[2-3 paragraphs: how the feature will be built. Key architectural decisions.]

## Components Affected

| Component | Change Type | Description |
|-----------|------------|-------------|
| [src/app/api/[resource]/route.ts] | New | [New API endpoint] |
| [src/services/[name].service.ts] | New | [Business logic] |
| [src/components/[Name]/] | New | [UI component] |
| [prisma/schema.prisma] | Modified | [Add new model/field] |

## Data Flow

```
[1. User triggers action in UI
 2. Component calls service function
 3. Service validates input and applies business logic
 4. Repository queries/updates database
 5. Response flows back through layers to UI]
```

## Database Changes

<!-- If this feature requires schema changes -->

```prisma
[model NewEntity {
  id        String   @id @default(cuid())
  name      String
  createdAt DateTime @default(now())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
}]
```

## API Design

<!-- If this feature adds/changes API endpoints -->

```
[POST /api/resource]
  Request:  { "name": "string", "type": "enum" }
  Response: { "data": { "id": "string", ... } }
  Auth:     Required
  Status:   201 Created / 400 Bad Request / 401 Unauthorized
```

## Error Handling

| Error Case | Response | User Experience |
|-----------|----------|-----------------|
| [Invalid input] | [400 + validation details] | [Show field errors] |
| [Not authorized] | [403] | [Redirect to login] |
| [Server error] | [500 + generic message] | [Show error page] |

## Testing Strategy

- Unit tests: [service logic, validation]
- Integration tests: [API endpoint, database queries]
- UI tests: [component rendering, user interactions]

## Security Considerations

- [Input validation at API boundary]
- [Authorization: user can only access own resources]
- [No sensitive data in URL parameters]
